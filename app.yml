name: Inject secret into HTML

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Inject secret into index.html
        run: |
          sed "s|__API_KEY__|${{ secrets.SECRET_TOKEN }}|g" index.template.html > index.html
